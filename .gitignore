# Dependencies
node_modules/

# Build output (regenerated by `npm run build` / postinstall on deploy)
dist/

# Secrets — NEVER commit. Real Telnyx/PayPal keys live here locally and in the
# host's environment variables in production. (.env.production & .env.example
# below are PUBLIC config and ARE committed.)
.env
.env.local
.env.*.local

# Logs & misc
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Tooling / editor / test artifacts
.playwright-mcp/
.vite/
*.tsbuildinfo
